import { IsUrlDirective } from './is-url.directive';

describe('IsUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new IsUrlDirective();
    expect(directive).toBeTruthy();
  });
});
